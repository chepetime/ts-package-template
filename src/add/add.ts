/**
 * Add
 * @description Adds two provided numbers together & returns the resulting value
 * @param a - First Number
 * @param b - Second Number
 */
export function add(a: number, b: number) {
  return a + b;
}
